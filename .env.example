# Database Configuration
POSTGRES_DB=canteen_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Backend Configuration (Internal only - not exposed to host)
SERVER_PORT=8080

# Frontend Configuration
FRONTEND_PORT=3000              # Host machine port (external access)
FRONTEND_INTERNAL_PORT=80       # Container internal port (nginx listens on)